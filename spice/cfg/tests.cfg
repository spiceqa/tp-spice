# Do not edit this file

# This file holds all possible tests variants useful for SpiceQE team

include tests-shared.cfg

# Filters `only'

only (image_backend=filesystem)
only no_9p_export
only no_virtio_rng
only no_pci_assignable
only smallpages
only default_bios
only bridge
only smp2
only qcow2
only virtio_net
only virtio_blk
only i440fx

# Filter `no'

no multi_host
no build_install

# run configuration
include run.cfg
